/*Для поиcка имени пользователя мы создадим хэш коллекцию*/

HSET emails '<имя_пользователя>' '<почта>'

/* где вместо <имя_пользователя> будем подставлять имена пользователей,
а вместо <почта> - email адреса*/

/*Для поиска электронного адреса (не смогла найти как искать ключи)
 поэтому решила создать второй такой же хэш, но наоорот*/

HSET users '<почта>' '<имя_пользователя>'

/* где также вместо <имя_пользователя> будем подставлять имена пользователей,
а вместо <почта> - email адреса*/